import Button from "./Button/Button"

# Molecules

Molecules are groups of atoms that form more complex UI units. They encapsulate small compositions while remaining reusable.

Examples

- `DialogHeader` combining a `Title` atom and a `CloseButton` atom
- `FormField` combining `Label`, `Input`, and `HelperText`

Demo

<Button>Example molecule trigger</Button>
